# .git-pm/config.yaml
# Project-level configuration (overrides user config for this project)
# Team can decide whether to commit this or not

# Override packages directory for this project
packages_dir: .git-packages

# Project-specific URL patterns
url_patterns:
  internal.company.com: "https://git.internal.company.com/{path}.git"

# Force HTTPS for all git operations in this project
git_protocol:
  github.com: https
  gitlab.com: https

# Disable auto-update for this project (require explicit update command)
auto_update_branches: false

# Project-specific cache location (optional)
# cache_dir: ./.git-pm-cache
